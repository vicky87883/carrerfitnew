"use client";
import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";
import AppNav from "../../components/AppNav";
type Status = { configured: boolean; authenticated: boolean };
export default function AdminPage() {
  const [status, setStatus] = useState<Status | null>(null); const [email, setEmail] = useState(""); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [notice, setNotice] = useState(""); const [error, setError] = useState(""); const [stats, setStats] = useState<Record<string, number> | null>(null);
  async function call(path: string, init?: RequestInit) { const res = await fetch(path, { ...init, headers: { "Content-Type": "application/json", ...init?.headers } }); const body = await res.json().catch(() => ({})); if (!res.ok) throw new Error(body.message || "Request failed"); return body; }
  async function load() { try { const result = await call("/api/admin/status"); setStatus(result); if (result.authenticated) { const data = await call("/api/admin/overview"); setStats(data.stats); } } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not open administrator access."); } }
  useEffect(() => { void load(); }, []);
  async function submit(event: FormEvent) { event.preventDefault(); setError(""); try { const result = await call("/api/admin/request-access", { method: "POST", body: JSON.stringify({ email, username, password }) }); setNotice(result.message); setPassword(""); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not request access."); } }
  return <main className="adminPage"><AppNav/><section className="adminPanel"><p className="adminEyebrow">Private administrator portal</p><h1>Administrator sign in</h1>{error && <p className="adminMessage error" role="alert">{error}</p>}{notice && <p className="adminMessage">{notice}</p>}{status && !status.configured && <p className="adminMessage error">Set ADMIN_EMAIL, ADMIN_USERNAME, and ADMIN_PASSWORD in Hostinger, then redeploy.</p>}{status?.configured && !status.authenticated && <form className="adminForm" onSubmit={submit}><label>Administrator email <input type="email" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} required/></label><label>Username <input autoComplete="username" value={username} onChange={(event) => setUsername(event.target.value)} required/></label><label>Password <input type="password" autoComplete="current-password" value={password} onChange={(event) => setPassword(event.target.value)} required/></label><button>Send confirmation link</button><p><small>We will send a confirmation link only to the configured administrator mailbox.</small></p></form>}{status?.authenticated && <div><p>Administrator access is confirmed for this browser.</p><div className="gridCards">{Object.entries(stats || {}).map(([key, value]) => <article key={key}><strong>{value}</strong><span>{key.replace(/([A-Z])/g, " $1")}</span></article>)}</div><p><Link href="/job-sources">Manage job sources</Link> · <Link href="/blog-admin">Manage blog posts</Link></p></div>}</section></main>;
}
