import { siteUrl } from "@/lib/site";
import { listPublishedBlogPosts } from "@/server/blog-store";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function GET() { const posts = await listPublishedBlogPosts({ limit: 50 }); const items = posts.map(post => `<item><title>${xml(post.title)}</title><link>${xml(siteUrl(`/blog/${post.slug}`))}</link><guid isPermaLink="true">${xml(siteUrl(`/blog/${post.slug}`))}</guid><description>${xml(post.excerpt)}</description><category>${xml(post.category)}</category><pubDate>${new Date(post.publishedAt || post.createdAt).toUTCString()}</pubDate></item>`).join(""); const feed = `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>CarrerFit Career Guides</title><link>${xml(siteUrl("/blog"))}</link><description>Practical guides for resumes, interviews, career changes, and focused job searches.</description><language>en-in</language><lastBuildDate>${new Date().toUTCString()}</lastBuildDate>${items}</channel></rss>`; return new Response(feed, { headers: { "Content-Type": "application/rss+xml; charset=utf-8", "Cache-Control": "public, max-age=0, s-maxage=300, stale-while-revalidate=3600" } }); }
function xml(value: string) { return value.replace(/[<>&'"]/g, character => ({ "<": "&lt;", ">": "&gt;", "&": "&amp;", "'": "&apos;", '"': "&quot;" })[character]!); }
